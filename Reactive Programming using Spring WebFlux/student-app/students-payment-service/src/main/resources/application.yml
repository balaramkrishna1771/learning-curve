spring:
  application:
    name: students-payment-service
  config:
    import: "configserver:http://localhost:8888"
  cloud:
    config:
      uri: http://localhost:8888
      name: students-payment-service, shared-config